apiVersion: v1
kind: Service
metadata:
  name: {{ include "central-helm.fullname" . }}
  labels:
    {{- include "central-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "central-helm.selectorLabels" . | nindent 4 }}
